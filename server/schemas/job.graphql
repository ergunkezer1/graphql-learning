type Query {
    job: [Job!]!
}

type Job {
    id: ID!
    title: String!
    """
    This __date__ when the job is published in ISO-8601 format. E.g `12-02-2025`
    """
    date: String!
    company: Company
    description: String
}
